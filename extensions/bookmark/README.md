# Bookmark

Save agent responses for later.

```text
/bookmark         Save the last agent response on the current branch
/bookmark show    Browse all bookmarks in the current session
/bookmark help    Show usage
```

## Storage

Bookmarks live in `~/.local/share/bookmark-pi/<session-id>/<entry-id>.json`.
